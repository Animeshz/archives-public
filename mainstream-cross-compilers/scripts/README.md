Setup files to run before invoking compiler for environment to setup for that target.

After running these, use `$CMAKE`, `$CC`, `$CXX`, `$AS`, `$AR`, `$LD`, `$FC` to invoke compiler and its respective components.